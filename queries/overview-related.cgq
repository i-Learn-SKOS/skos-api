contexts/common-context.json
===
{
  "ID": "%collection%"
}
===
{
  TYPE(_:Collection)
  id(_:ID) @single
  prefLabel @single
  altLabel @optional
  definition @single @optional
  member @optional {
    id @single
    prefLabel @single
    altLabel @optional
    definition @single @optional
    related @optional {
      id @single
      memberOf @optional
      prefLabel @single
      altLabel @optional
      definition @single @optional
    }
  }
}
